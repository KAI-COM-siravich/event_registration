const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findFirst(); if(!user) return console.log('no user'); try { await prisma.user.update({where: {id: user.id}, data: {id: user.id + '_test'}}); console.log('cascade ok'); await prisma.user.update({where: {id: user.id + '_test'}, data: {id: user.id}}); } catch (e) { console.error('error', e.message); } } main().finally(() => prisma.$disconnect());
